---
title: "Agree the export format with the reviewer"
owner: bob
status: blocked
priority: high
created: 2026-08-04
blocked_by: bob — reviewing the proposal since 2026-08-04
group: export-review
---

## Context

The format cannot be chosen until the review comes back. This task is deliberately
`blocked` rather than `open`: the ball is on somebody else's side, and pretending
otherwise makes the board lie about what we can actually move today.

`blocked_by` names a person, not a state. "Waiting for feedback" would tell you nothing
about who to chase.

## Definition of done

A decision recorded in `status.md`, and this task moved to `open` or `done` accordingly.
